import React from 'react';

const NotMatch = () => {
    return (
        <div>
            <h3 style={{textAlign:'center'}}>Page <span style={{color:'red'}}>404</span> Not found</h3>
        </div>
    );
};

export default NotMatch;